<h1><%= @album.name %></h1>

<ul>
  <% @album.tracks.each do |track| %>
    <li><%= track_link(track) %></li>
  <% end %>
</ul>

<h4>Links</h4>
<ul>
  <li><%= link_to "New track", new_album_track_url(@album) %></li>
  <li><%= band_link @album.band %></li>
</ul>
